import { Shield, Clock, Euro, type LucideIcon } from "lucide-react"

/** Icônes des blocs « Pourquoi PitStop » — titres et textes via i18n (`marketing.feature*`). */
export const marketingFeatureIcons: LucideIcon[] = [Shield, Clock, Euro]
